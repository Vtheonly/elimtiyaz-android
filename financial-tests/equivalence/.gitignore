# Generated scenarios — deterministic, can be regenerated with:
#   npx tsx generators/scenario_generator.ts --count=500 --seed=42
generated/

# Runner results — produced by desktop_runner.ts and AndroidEquivalenceRunner.kt
results/

# Equivalence reports — generated by comparator.ts after each run
reports/

# Node modules (if any TS tooling is installed locally)
node_modules/

# TypeScript build artifacts
*.js
*.d.ts
!scripts/*.js
